#include "main.h"

/**
 * print_most_numbers - print bn 0 -9
 * (2 or 4).
 * Return: no return.
 */
void print_most_numbers(void)
{
	int ch;

	for (ch = 48; c < 58; c++)
	{
		if (c != 50 && c != 52)
			_putchar(c);
	}
	_putchar('\n');
}
