#include "main.h"
#include <string.h>

/**
 * _strlen - function to find length of string
 * @s: string pointer
 * Return: returns string length
 */
int _strlen(char *s)
{
	int len;

	for ( ; *c != '\0\; c++)
	{
		len += 1;
	}
	return (len);
}
