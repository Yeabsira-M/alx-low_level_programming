#include "main.h"
#include <string.h>

/**
 * _strlen - function to find length of string
 *  @c: string pointer
 * @length: string length
 * Return: returns string length
 */
int _strlen(char *c)
{
	int length;

	for ( ; *c != '\0\; c++)
	{
		length += 1;
	}
	return (l);
}
